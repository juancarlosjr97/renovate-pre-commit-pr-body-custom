{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "pre-commit": {
        "enabled": true,
        "prBodyNotes": [
            "{{#if (equals manager 'pre-commit')}} Testing if prBodyNotes {{/if}}"
        ]
    }
}
